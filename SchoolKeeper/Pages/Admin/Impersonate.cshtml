@page
@model SchoolKeeper.Pages.Admin.ImpersonateModel
@{
    ViewData["Title"] = "Вхід від імені користувача";
}

<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">
                        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" class="bi bi-person-badge me-2" viewBox="0 0 16 16">
                            <path d="M6.5 2a.5.5 0 0 0 0 1h3a.5.5 0 0 0 0-1h-3zM11 8a3 3 0 1 1-6 0 3 3 0 0 1 6 0z"/>
                            <path d="M4.5 0A2.5 2.5 0 0 0 2 2.5v14.5A1.5 1.5 0 0 0 3.5 18h9a1.5 1.5 0 0 0 1.5-1.5v-14.5A2.5 2.5 0 0 0 11.5 0h-7zM3 2.5A1.5 1.5 0 0 1 4.5 1h7A1.5 1.5 0 0 1 13 2.5v14.5a.5.5 0 0 1-.5.5h-9a.5.5 0 0 1-.5-.5V2.5z"/>
                        </svg>
                        Вхід від імені користувача
                    </h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-info">
                        <strong>Увага!</strong> Ви будете працювати від імені обраного користувача. Всі дії будуть виконуватися від його імені.
                    </div>

                    <form id="impersonateForm">
                        <div class="mb-3">
                            <label for="userId" class="form-label">Оберіть користувача *</label>
                            <select class="form-select" id="userId" name="userId" required>
                                <option value="">Завантаження користувачів...</option>
                            </select>
                            <small class="form-text text-muted">Виберіть користувача, від імені якого хочете увійти</small>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary btn-lg" id="impersonateBtn">
                                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" class="bi bi-box-arrow-in-right me-2" viewBox="0 0 16 16">
                                    <path fill-rule="evenodd" d="M6 3.5a.5.5 0 0 1 .5-.5h8a.5.5 0 0 1 .5.5v9a.5.5 0 0 1-.5.5h-8a.5.5 0 0 1-.5-.5v-2a.5.5 0 0 0-1 0v2A1.5 1.5 0 0 0 6.5 14h8a1.5 1.5 0 0 0 1.5-1.5v-9A1.5 1.5 0 0 0 14.5 2h-8A1.5 1.5 0 0 0 5 3.5v2a.5.5 0 0 0 1 0v-2z"/>
                                    <path fill-rule="evenodd" d="M11.854 8.354a.5.5 0 0 0 0-.708l-3-3a.5.5 0 1 0-.708.708L10.293 7.5H1.5a.5.5 0 0 0 0 1h8.793l-2.147 2.146a.5.5 0 0 0 .708.708l3-3z"/>
                                </svg>
                                Увійти від імені користувача
                            </button>
                            <a href="/Admin" class="btn btn-secondary">
                                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" class="bi bi-arrow-left me-2" viewBox="0 0 16 16">
                                    <path fill-rule="evenodd" d="M15 8a.5.5 0 0 0-.5-.5H2.707l3.147-3.146a.5.5 0 1 0-.708-.708l-4 4a.5.5 0 0 0 0 .708l4 4a.5.5 0 0 0 .708-.708L2.707 8.5H14.5A.5.5 0 0 0 15 8z"/>
                                </svg>
                                Назад до адмін-панелі
                            </a>
                        </div>
                    </form>

                    <div id="errorAlert" class="alert alert-danger mt-3" style="display: none;">
                        <strong>Помилка!</strong> <span id="errorMessage"></span>
                    </div>
                </div>
            </div>

            @if (Model.IsImpersonating)
            {
                <div class="card shadow mt-4 border-warning">
                    <div class="card-header bg-warning text-dark">
                        <h5 class="mb-0">Режим входу від імені користувача активний</h5>
                    </div>
                    <div class="card-body">
                        <p class="mb-2">
                            <strong>Ви працюєте від імені:</strong> @Model.ImpersonatedUserEmail<br>
                            <strong>Роль:</strong> <span class="badge bg-info">@Model.ImpersonatedUserRole</span>
                        </p>
                        <button type="button" class="btn btn-warning" id="stopImpersonationBtn">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-x-circle me-1" viewBox="0 0 16 16">
                                <path d="M8 15A7 7 0 1 1 8 1a7 7 0 0 1 0 14zm0 1A8 8 0 1 0 8 0a8 8 0 0 0 0 16z"/>
                                <path d="M4.646 4.646a.5.5 0 0 1 .708 0L8 7.293l2.646-2.647a.5.5 0 0 1 .708.708L8.707 8l2.647 2.646a.5.5 0 0 1-.708.708L8 8.707l-2.646 2.647a.5.5 0 0 1-.708-.708L7.293 8 4.646 5.354a.5.5 0 0 1 0-.708z"/>
                            </svg>
                            Вийти з режиму входу від імені
                        </button>
                    </div>
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', async function() {
            const userIdSelect = document.getElementById('userId');
            const impersonateForm = document.getElementById('impersonateForm');
            const errorAlert = document.getElementById('errorAlert');
            const errorMessage = document.getElementById('errorMessage');
            const impersonateBtn = document.getElementById('impersonateBtn');

            // Завантажуємо список користувачів
            try {
                const { response, data } = await auth.apiRequest('/User?pageSize=1000');
                if (response.ok && data.data) {
                    userIdSelect.innerHTML = '<option value="">Оберіть користувача</option>';
                    data.data.forEach(user => {
                        const option = document.createElement('option');
                        option.value = user.id;
                        option.textContent = `${user.fullName} (${user.email}) - ${user.role}`;
                        userIdSelect.appendChild(option);
                    });
                } else {
                    userIdSelect.innerHTML = '<option value="">Помилка завантаження користувачів</option>';
                }
            } catch (error) {
                console.error('Error loading users:', error);
                userIdSelect.innerHTML = '<option value="">Помилка завантаження користувачів</option>';
            }

            // Обробка форми
            impersonateForm.addEventListener('submit', async function(e) {
                e.preventDefault();
                
                const userId = userIdSelect.value;
                if (!userId) {
                    showError('Будь ласка, оберіть користувача');
                    return;
                }

                errorAlert.style.display = 'none';
                impersonateBtn.disabled = true;
                impersonateBtn.innerHTML = '<span class="spinner-border spinner-border-sm me-2"></span>Вхід...';

                try {
                    const { response, data } = await auth.apiRequest('/auth/impersonate', {
                        method: 'POST',
                        body: JSON.stringify({ userId: parseInt(userId) })
                    });

                    if (response.ok && data.statusCode === 200) {
                        // Зберігаємо дані користувача
                        localStorage.setItem('authToken', data.data.token);
                        localStorage.setItem('userEmail', data.data.email);
                        localStorage.setItem('userRole', data.data.role);
                        localStorage.setItem('userId', data.data.userId);
                        localStorage.setItem('isImpersonating', 'true');
                        localStorage.setItem('originalAdminId', data.data.originalAdminId || '');
                        
                        // Встановлюємо cookie
                        document.cookie = `authToken=${data.data.token}; path=/; max-age=43200`;
                        document.cookie = `isImpersonating=true; path=/; max-age=43200`;
                        document.cookie = `originalAdminId=${data.data.originalAdminId || ''}; path=/; max-age=43200`;
                        
                        // Перенаправляємо на головну
                        window.location.href = '/';
                    } else {
                        showError(data.message || 'Помилка входу від імені користувача');
                        impersonateBtn.disabled = false;
                        impersonateBtn.innerHTML = '<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" class="bi bi-box-arrow-in-right me-2" viewBox="0 0 16 16"><path fill-rule="evenodd" d="M6 3.5a.5.5 0 0 1 .5-.5h8a.5.5 0 0 1 .5.5v9a.5.5 0 0 1-.5.5h-8a.5.5 0 0 1-.5-.5v-2a.5.5 0 0 0-1 0v2A1.5 1.5 0 0 0 6.5 14h8a1.5 1.5 0 0 0 1.5-1.5v-9A1.5 1.5 0 0 0 14.5 2h-8A1.5 1.5 0 0 0 5 3.5v2a.5.5 0 0 0 1 0v-2z"/><path fill-rule="evenodd" d="M11.854 8.354a.5.5 0 0 0 0-.708l-3-3a.5.5 0 1 0-.708.708L10.293 7.5H1.5a.5.5 0 0 0 0 1h8.793l-2.147 2.146a.5.5 0 0 0 .708.708l3-3z"/></svg>Увійти від імені користувача';
                    }
                } catch (error) {
                    console.error('Error impersonating:', error);
                    showError('Помилка підключення до сервера');
                    impersonateBtn.disabled = false;
                    impersonateBtn.innerHTML = '<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" class="bi bi-box-arrow-in-right me-2" viewBox="0 0 16 16"><path fill-rule="evenodd" d="M6 3.5a.5.5 0 0 1 .5-.5h8a.5.5 0 0 1 .5.5v9a.5.5 0 0 1-.5.5h-8a.5.5 0 0 1-.5-.5v-2a.5.5 0 0 0-1 0v2A1.5 1.5 0 0 0 6.5 14h8a1.5 1.5 0 0 0 1.5-1.5v-9A1.5 1.5 0 0 0 14.5 2h-8A1.5 1.5 0 0 0 5 3.5v2a.5.5 0 0 0 1 0v-2z"/><path fill-rule="evenodd" d="M11.854 8.354a.5.5 0 0 0 0-.708l-3-3a.5.5 0 1 0-.708.708L10.293 7.5H1.5a.5.5 0 0 0 0 1h8.793l-2.147 2.146a.5.5 0 0 0 .708.708l3-3z"/></svg>Увійти від імені користувача';
                }
            });

            function showError(message) {
                errorMessage.textContent = message;
                errorAlert.style.display = 'block';
            }

            // Обработка выхода из режима impersonation
            const stopImpersonationBtn = document.getElementById('stopImpersonationBtn');
            if (stopImpersonationBtn) {
                stopImpersonationBtn.addEventListener('click', async function() {
                    stopImpersonationBtn.disabled = true;
                    stopImpersonationBtn.innerHTML = '<span class="spinner-border spinner-border-sm me-1"></span>Вихід...';

                    try {
                        // Получаем originalAdminId из localStorage
                        const originalAdminId = localStorage.getItem('originalAdminId');
                        const { response, data } = await auth.apiRequest('/auth/stop-impersonation', {
                            method: 'POST',
                            body: JSON.stringify({ originalAdminId: originalAdminId || '' })
                        });

                        if (response.ok && data.statusCode === 200) {
                            // Обновляем данные админа в localStorage
                            localStorage.setItem('authToken', data.data.token);
                            localStorage.setItem('userEmail', data.data.email);
                            localStorage.setItem('userRole', data.data.role);
                            localStorage.setItem('userId', data.data.userId);
                            localStorage.removeItem('isImpersonating');
                            localStorage.removeItem('originalAdminId');
                            
                            // Обновляем cookie
                            document.cookie = `authToken=${data.data.token}; path=/; max-age=43200`;
                            document.cookie = 'isImpersonating=; path=/; expires=Thu, 01 Jan 1970 00:00:00 GMT';
                            document.cookie = 'originalAdminId=; path=/; expires=Thu, 01 Jan 1970 00:00:00 GMT';
                            
                            // Перенаправляем на админ-панель
                            window.location.href = '/Admin';
                        } else {
                            showError(data.message || 'Помилка виходу з режиму входу від імені');
                            stopImpersonationBtn.disabled = false;
                            stopImpersonationBtn.innerHTML = '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-x-circle me-1" viewBox="0 0 16 16"><path d="M8 15A7 7 0 1 1 8 1a7 7 0 0 1 0 14zm0 1A8 8 0 1 0 8 0a8 8 0 0 0 0 16z"/><path d="M4.646 4.646a.5.5 0 0 1 .708 0L8 7.293l2.646-2.647a.5.5 0 0 1 .708.708L8.707 8l2.647 2.646a.5.5 0 0 1-.708.708L8 8.707l-2.646 2.647a.5.5 0 0 1-.708-.708L7.293 8 4.646 5.354a.5.5 0 0 1 0-.708z"/></svg>Вийти з режиму входу від імені';
                        }
                    } catch (error) {
                        console.error('Error stopping impersonation:', error);
                        showError('Помилка підключення до сервера');
                        stopImpersonationBtn.disabled = false;
                        stopImpersonationBtn.innerHTML = '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-x-circle me-1" viewBox="0 0 16 16"><path d="M8 15A7 7 0 1 1 8 1a7 7 0 0 1 0 14zm0 1A8 8 0 1 0 8 0a8 8 0 0 0 0 16z"/><path d="M4.646 4.646a.5.5 0 0 1 .708 0L8 7.293l2.646-2.647a.5.5 0 0 1 .708.708L8.707 8l2.647 2.646a.5.5 0 0 1-.708.708L8 8.707l-2.646 2.647a.5.5 0 0 1-.708-.708L7.293 8 4.646 5.354a.5.5 0 0 1 0-.708z"/></svg>Вийти з режиму входу від імені';
                    }
                });
            }

            // Обработка параметра stop в URL (если был переход через OnPostStopImpersonationAsync)
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.get('stop') === 'true') {
                // Автоматически вызываем выход из режима impersonation
                if (stopImpersonationBtn) {
                    stopImpersonationBtn.click();
                }
            }
        });
    </script>
}

